def find_multiples(integer, limit):
    
    return list(range(integer, limit+1,integer))

#range(start, end, amount of increase)
#so it's look like this 5 to 25 in indexes of course but 25 is not include so we add +1